{
  "name": "functions",
  "version": "1.0.0",
  "description": "Cloud Functions for CV Builder",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@sparticuz/chromium": "^143.0.0",
    "2captcha": "^3.0.5-2",
    "axios": "^1.13.2",
    "cheerio": "^1.1.2",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.5.0",
    "puppeteer": "^24.34.0",
    "puppeteer-core": "^24.34.0",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
